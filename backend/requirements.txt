fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pydantic==2.10.4
python-multipart==0.0.20
httpx==0.28.1
# Needed whenever DATABASE_URL points at Postgres, which it does on Render.
# Locally the default is SQLite and this simply goes unused.
psycopg2-binary==2.9.10
pytest==8.3.4

# Optional: local speech synthesis via Meta MMS, for languages Khaya cannot
# reach and for when its quota is exhausted. The app runs without these -- the
# pipeline reports the route as unavailable rather than failing.
#   pip install torch transformers "numpy<2"
# numpy is pinned below 2 because torch 2.2 is built against the 1.x ABI.
