# Where the API lives. Leave blank for local development -- Vite proxies /api
# to 127.0.0.1:8000 so no value is needed.
#
# On Vercel, set this to the Render service address:
VITE_API_BASE=https://mabia-api.onrender.com
